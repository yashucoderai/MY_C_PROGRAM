#include <stdio.h>

int main() {
    int a,b, area;
    printf("enter the value of a");
    scanf("%d", &a);
   
    printf("enter the value of b");
    scanf("%d", &b);
    area=a*b;
    printf("area of rectangle %d", area);

    return 0;
}
